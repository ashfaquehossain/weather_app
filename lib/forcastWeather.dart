import 'package:flutter/material.dart';

class ForcastWeather extends StatelessWidget {
  const ForcastWeather({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Forcast Weather"),
    );
  }
}
